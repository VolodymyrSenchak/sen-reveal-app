export const environment = {
  production: false,
  /** sen-reveal-api dev server (`npm start` in that repo defaults to 8080). */
  apiUrl: 'http://localhost:8080',
  //apiUrl: 'https://sen-reveal-api.vercel.app',
};
